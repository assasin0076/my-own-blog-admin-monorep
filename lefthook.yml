pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm lint

    stylelint:
      run: pnpm f stylelint

    format:
      run: pnpm format

    types:
      run: pnpm types
commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
