pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm lint

    format:
      run: pnpm format

    types:
      run: pnpm types
commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
