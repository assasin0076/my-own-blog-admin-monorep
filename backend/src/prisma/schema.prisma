generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Stuff {
  id          String  @id @default(uuid())
  label       String  @unique
  description String
  tags        String
  repoLink    String
  viewLink    String?
}
