{
  "name": "@my-own-blog-admin-pannel/shared",
  "private": true,
  "version": "0.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    "./pick": "./src/pick.ts",
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "jest",
    "types": "tsc --noEmit --project ./tsconfig.json",
    "lint": "eslint --cache --cache-location ./node_modules/.cache/.eslintcache --ext .ts .",
    "prettify": "prettier --loglevel warn --cache --write '{src/**/*,*}.{ts,tsx,js,json,yml,scss}' '!**/{node_modules,dist}/**/*'",
    "build": "tsc -b"
  },
  "dependencies": {
    "lodash": "^4.18.1",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "@types/lodash": "^4.17.24",
    "@types/node": "20.3.1"
  }
}
